# '''
# Syncopation means an emphasis on a weak beat of a bar of music; most commonly,
# beats 2 and 4 (and all other even-numbered beats if applicable).
#
# s is a line of music, represented as a string, where hashtags # represent e
# mphasized beats. Create a function that returns if the line of music contains any syncopation.
# '''
#
#
# def has_syncopation(txt):
#     return any([i % 2 == 1 for i, j in enumerate(txt) if j == '#'])
#
# print(has_syncopation("#.#.###."))



# print('1234567'[:-2])
